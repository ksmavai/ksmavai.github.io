# personal website

my little corner of the internet. built this using **next.js**, **react**, and **tailwindcss**. it's designed to be clean, fast, and easy to update using **markdown** files.

## 🚀 key features

*   **modern stack**: built with the latest web tech including next.js app router and react server components.
*   **beautiful styling**: custom clean design using tailwindcss with dark/light mode support.
*   **content management**: writes notes and pages in markdown, automatically rendered to the site.

## 🏃‍♂️ how to run

### local development
1.  **install dependencies**:
    ```bash
    npm install
    ```

2.  **run the dev server**:
    ```bash
    npm run dev
    ```
    open [http://localhost:3000](http://localhost:3000) to see the site.

## 📦 deployment

hosted on **vercel** (or whatever uses `next build`).
updates are automatically deployed when pushing to main.
